export interface Race {
  raceName: string;
	raceDate: Date;
	distances: [Array: Number];
  local: string,
	// 	startAdress: string;
	// 	city: string;
	// 	estate: string
	// };
  link: string;
}